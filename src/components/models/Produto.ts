export default interface Postagem {
    id: number;
    nome: string;
    descricao: string;
    quantidade: string;
    preco: number;
    saudavel: string;
    // categoria: Categoria | null;
    // usuario: Usuario | null;
}